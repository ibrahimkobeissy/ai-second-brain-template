# To Check — Job Hunt

Raw links/tools to triage into `draft/` (via `curate-bookmarks`) or verify (via `scout-idea`). Part of [[job-hunt]].

> 💡 **Triage lens:** for each link ask *what can we steal* — a pattern, prompt, data model, or workflow — and which part of the hunt it serves. Once acted on, delete the line (git keeps history).

- [ ] *(drop links here)*
