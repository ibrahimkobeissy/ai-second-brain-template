# To Check — Standup Tools

Raw links/items to triage into `draft/` (via `curate-bookmarks`) or verify (via `scout-idea`). Add **one `- [ ]` per item** (URL + short `—` note). Once an item is curated or acted on, delete its line — git keeps the history. Don't leave empty checkboxes; they show as blank tasks on the [[dashboard]].
Part of [[standup-tools]].
