<!-- FORMAT of what calibrate-comms writes BETWEEN the COMMS-PROFILE markers in CLAUDE.md.
     One line per active directive. Omit any axis left at a neutral/MID default, to keep the block lean.
     Keep it short — this loads into every agent's context every session. -->

**How to write to {{user}}** — *calibrated {{today}} via `/calibrate-comms` (depth: {{quick_or_deep}})*

- {{density directive line}}
- {{sequence directive line}}
- {{modality directive line}}
- {{abstraction directive line}}
- {{tradeoff directive line}}
- {{detail directive line}}
- {{jargon directive line — global default; an Area's hub note may override}}
- {{tone directive line}}
- {{context-giving directive line}}

*Calibration, not classification — correct any line by telling me, or re-run `/calibrate-comms`.*
